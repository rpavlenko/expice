import * as styles from './PostListTitle.module.scss';

export default function PostListTitle() {
  return (
    <div>
      <h1 className={styles.postListTitle}>Most Recent Posts</h1>
    </div>
  );
}
